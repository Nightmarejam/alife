# Roadmap — the one place to reorient

*When you float back in and don't know where you are, read this. It holds the thread so you don't have
to. Honest phase-based timeframes, not calendar dates — your cadence is intermittent and that's fine.*

## The arc (three layers, one goal)

```
ALife sandbox  →  Constella (constitution)  →  FAITHH (the model)
  (science)          (policy, receipt-backed)     (the companion that runs on it)
   receipts           grounded in the receipts     trained on the constitution
```

You are building a constitution that argues from **evidence** and from the **heart**, then training a
companion on it. The sandbox earns the words; the constitution holds them; the model learns them.

## Where you are right now (2026-07-10)

| phase | status | how far |
|---|---|---|
| 1. **Science** — earn confirmed words | 🟢 in progress | **~15 / 20** headline words. C-batch (C1–C4) done. Sandbox is mature. |
| 2. **Constitution** — ground + flesh out Constella | 🟢 healthy, keeps pace | 7 sync PRs; ledger has zero drift; new pair (Temporal Floor + Cadence) just added a whole dimension |
| 3. **Dataset** — turn constitution → training data | ⚪ not started (fine) | gated by the Oregon rig (~Jul 16) + the word-schema (✅ done). Nothing to do here until the rig. |
| 4. **Pilot fine-tune** — prove the pipeline small | ⚪ not started | can happen *early*, once a small dataset seed exists. De-risks everything. |
| 5. **Full FAITHH training** — the real run | ⚪ future | gated by ~20 words + a real dataset + the rig |

**The honest timeframe:** the model phase (3–5) is **weeks out no matter what**, because it needs the rig
and the dataset. So you have *runway*. There is no clock forcing you — floating now costs nothing. The
only "long pole" is the confirmed-word count in phase 1, and that grows a couple words per focused batch.

## What gates what (so you don't worry about the wrong thing)

- Phase 3 (dataset) can't really start until the rig (~16th) — so pre-16th, **stay in phases 1–2** (science
  + constitution). That's not falling behind; it's the correct order.
- Phase 5 (real model) waits on ~20 words. Phase 4 (pilot) does **not** — you can prove the pipeline early.
- Nothing is blocked by *you* right now. The work is accumulating on schedule.

## When you float back — start here

1. Read the **RESUME POINT** at the top of `EXPERIMENT_QUEUE.md` — it names the exact next experiment.
2. Read the **one recommended next action** at the bottom of the last session's summary.
3. If neither feels right, that's fine — **rest is legitimate** (it's literally your own Temporal Floor:
   fallow time is the Yin-return, not wasted time). Come back when you come back; the docs will hold.

## The single next action (updated each session)

> **As of 2026-07-11:** still ~16/20 words (the gamer-lifecycle attempt was an honest negative — didn't
> reproduce in b2; banked). Still pre-rig. Best next word candidate: **mine `proof_of_life_consensus.md`**
> (un-touched governance doc — likely a distinct mechanism to test) OR a **dedicated Exp-8b/9 NAKED/DEFENDER
> replica** to properly test the gamer-lifecycle. Alt (heart track): revise the Temporal-Floor / Cadence
> drafts. Pick whichever pulls; if nothing does, float — there's runway.
